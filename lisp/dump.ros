#!/bin/sh
#|-*- mode:lisp -*-|#
#|
exec ros -Q -- $0 "$@"
|#
(progn ;;init forms
  (ros:ensure-asdf)
  #+quicklisp(ql:quickload '() :silent t)
  )

(defpackage :ros.script.dump.3837499522
  (:use :cl))
(in-package :ros.script.dump.3837499522)



(defun main (&rest argv)
  (declare (ignorable argv))
  (loop (let ((num (read-byte *standard-input* nil)))
          (if (not num) (return)
              (format t "~2,'0x " num)))))
;;; vim: set ft=lisp lisp:
