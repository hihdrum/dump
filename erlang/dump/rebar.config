{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps,
 [dump]}.
{escript_main_app, dump}.
{escript_name, dump}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
